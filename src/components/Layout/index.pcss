.layout {
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    padding: 0;
    max-width: 1000px;
    margin: 0 auto;

    &__main {
        flex-grow: 1;
        padding: 0;
    }
}
